rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Landing page content: publicly readable, only editable by signed-in admins.
    match /content/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
